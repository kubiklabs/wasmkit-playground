import { Flex } from "@chakra-ui/react";
import TabGroup from "../navbar-components/TabGroup";

const Navbar = () => {
  return (
    <Flex>
      <TabGroup />
    </Flex>
  );
};

export default Navbar;
